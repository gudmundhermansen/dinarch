% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dinarch-class.R
\name{predict.dinarch_fit}
\alias{predict.dinarch_fit}
\title{Predict from a fitted DINARCH model}
\usage{
\method{predict}{dinarch_fit}(
  object,
  horizon = NULL,
  nsim = 1000,
  new_data = NULL,
  use_draw = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{"dinarch_fit"} object.}

\item{horizon}{\code{NULL} (default) predicts in-sample; a positive integer
forecasts that many periods ahead - see \code{\link[=dinarch_project]{dinarch_project()}}.}

\item{nsim}{Number of predictive draws. Default 1000 (higher than
\code{\link[=dinarch_project]{dinarch_project()}}'s default of 1, since the point of \code{predict()} is
to characterize the predictive distribution).}

\item{new_data}{Optional forward/alternative covariate values - see
\code{\link[=dinarch_project]{dinarch_project()}}.}

\item{use_draw}{For Bayesian fits: \code{NULL} (default) uses the posterior
mean for every replicate; \code{"random"} samples a fresh posterior draw
per replicate, propagating full posterior uncertainty into the
predictive draws on top of the observation-level noise; an integer
pins one specific draw for every replicate. Must stay \code{NULL} for ML
fits (any other value errors - there is no posterior to draw from).}

\item{seed}{Optional random seed.}

\item{...}{Passed through to \code{\link[=dinarch_project]{dinarch_project()}} (e.g. \code{new_para},
\code{y_threshold}, for scenario-style overrides).}
}
\value{
A \code{data.table} (also classed \code{"dinarch_project"}, so \code{\link[=plot]{plot()}}
and \code{\link[=summary.dinarch_project]{summary.dinarch_project()}} both work on it directly), one row
per \verb{(group, index, sim)} predictive draw.
}
\description{
Draws \code{nsim} samples from the model's predictive distribution via
\code{\link[=dinarch_project]{dinarch_project()}} - a thin wrapper exposing only the arguments that
are reasonable to change for genuine prediction (as opposed to
constructing a scenario). Each replicate is one draw of the future (or,
with \code{horizon = NULL}, in-sample) path, combining parameter uncertainty
(via \code{use_draw}, for Bayesian fits) with observation-level
negative-binomial noise. Summarize the result with
\code{\link[=summary.dinarch_project]{summary.dinarch_project()}} for a compact mean/prediction-interval
table, or pass it to \code{\link[=plot]{plot()}} directly.
}
\details{
\code{\link[=dinarch_project]{dinarch_project()}}'s \code{new_para} (per-period \code{b}/\code{phi}/\code{beta}
overrides, e.g. from \code{\link[=dinarch_transition_new_para]{dinarch_transition_new_para()}} or
\code{\link[=dinarch_transition_path]{dinarch_transition_path()}}) and \code{y_threshold} (a rolling-window
escalation cap) both describe \emph{scenario} constructions
rather than draws from the fitted model's own predictive distribution,
so they are not named arguments here - call \code{\link[=dinarch_project]{dinarch_project()}}
directly if you need them (they can still be passed through \code{...}).
}
