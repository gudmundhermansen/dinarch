% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dinarch-fit.R
\name{dinarch_fit}
\alias{dinarch_fit}
\title{Fit a DINARCH model}
\usage{
dinarch_fit(data, ..., method = c("ml", "bayes"))
}
\arguments{
\item{data}{A data.frame or data.table; see \code{\link[=dinarch_fit_ml]{dinarch_fit_ml()}}.}

\item{...}{Further arguments passed to \code{\link[=dinarch_fit_ml]{dinarch_fit_ml()}} (if
\code{method = "ml"}) or \code{\link[=dinarch_fit_bayes]{dinarch_fit_bayes()}} (if \code{method = "bayes"}),
e.g. \code{y}, \code{index}, \code{group}, \code{formula}, \code{n_lags}, plus any
method-specific arguments.}

\item{method}{\code{"ml"} (default) for maximum-likelihood estimation via
\code{\link[=dinarch_fit_ml]{dinarch_fit_ml()}}, or \code{"bayes"} for Bayesian estimation via
\code{\link[=dinarch_fit_bayes]{dinarch_fit_bayes()}}.}
}
\value{
An object of class \code{"dinarch_fit"}; see \code{\link[=dinarch_fit_ml]{dinarch_fit_ml()}} /
\code{\link[=dinarch_fit_bayes]{dinarch_fit_bayes()}} for the full return value.
}
\description{
A thin dispatcher over \code{\link[=dinarch_fit_ml]{dinarch_fit_ml()}} and \code{\link[=dinarch_fit_bayes]{dinarch_fit_bayes()}},
giving a single entry point that switches on \code{method}. Method-specific
arguments (e.g. \code{optim_method}/\code{start}/\code{n_starts}/\code{control}/
\code{max_restarts} for \code{"ml"}; \code{prior}/\code{iter}/\code{chains}/\code{seed} for
\code{"bayes"}) are not duplicated here - they are documented on, and
forwarded via \code{...} straight through to, the underlying function, so
the two methods' argument sets can't drift out of sync with their real
implementations.
}
\examples{
sim <- dinarch_simulate(n = 200, b = 0.3, phi = 8, beta = log(4), seed = 1)
dat <- data.frame(y = sim$y, index = sim$index)

fit <- dinarch_fit(dat, n_lags = 1, method = "ml")
fit

}
