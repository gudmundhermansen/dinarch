% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dinarch-project.R
\name{plot.dinarch_project}
\alias{plot.dinarch_project}
\title{Plot a DINARCH projection}
\usage{
\method{plot}{dinarch_project}(
  x,
  type = c("ribbon", "spaghetti"),
  probs = c(0.1, 0.9),
  step = FALSE,
  ...
)
}
\arguments{
\item{x}{A \code{"dinarch_project"} object, i.e. the output of
\code{\link[=dinarch_project]{dinarch_project()}}.}

\item{type}{\code{"ribbon"} (default) or \code{"spaghetti"} - see above.}

\item{probs}{For \code{type = "ribbon"}, the lower/upper quantile
probabilities of the shaded band (default the 10th/90th percentile).}

\item{step}{If \code{TRUE}, draw step functions (\code{ggplot2::geom_step()},
holding each value flat until the next \code{index}) instead of straight
line segments between points - appropriate for count data, where a
straight line between two integers implies values that were never
observed. Default \code{FALSE}.}

\item{...}{Unused (kept for \code{plot()} generic compatibility).}
}
\value{
A \code{ggplot} object.
}
\description{
A minimal \code{ggplot2} plot of a \code{\link[=dinarch_project]{dinarch_project()}} result: the fitted
model's historical \code{y} (if available) as a single line, plus either
every simulated replicate as its own thin line (\code{type = "spaghetti"})
or a shaded quantile ribbon with a median line (\code{type = "ribbon"},
the default) summarizing the replicates at each \code{index}. Each group's
projected series is prepended with the last historical point strictly
before it, so the projection visually starts exactly where the
historical line ends instead of jumping in from a disconnected point.
One facet per group (skipped for a single-group/ungrouped fit). Returns
the \code{ggplot} object rather than just drawing it, so it prints like a
normal plot when called at the top level but can also be captured and
extended, e.g. \code{plot(proj) + ggplot2::ggtitle("Forecast")} - titles,
themes, and any other customization are deliberately left to the
caller rather than built in here.
}
